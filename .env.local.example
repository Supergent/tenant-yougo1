# Convex (ALWAYS REQUIRED)
# Get these values from: https://dashboard.convex.dev
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Better Auth (ALWAYS REQUIRED)
# Generate secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=
SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AI Provider (Agent Component - Optional)
# Only needed if you plan to add AI features to your todo app
# Choose ONE of the following:
OPENAI_API_KEY=
# OR
ANTHROPIC_API_KEY=

# Rate Limiter (No configuration needed)
# Rate limiting is configured in code, no environment variables required
